name: .NET Build and Test

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 * * * *' # Executa a cada hora


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    # Executar script personalizado
    - name: Custom Script
      run: |
        echo 'executando um script personalizado'